# Gitea Actions CI for ArrJanitor.
#
# INERT UNTIL CONFIGURED. This workflow does nothing until BOTH of the following
# exist:
#   1. A registered Gitea Actions runner able to pick up `ubuntu-latest` jobs.
#   2. A repository/organisation secret named `GH_TOKEN` holding a GitHub
#      Personal Access Token with *read* access to the private dependency repos
#      `mjblack/sonarr` and `mjblack/qbittorrent.cr`. `shards install` fetches
#      these from GitHub over HTTPS, so without the token the install step fails.
#
# Crystal is pinned to 1.20.2 on purpose: the dev dependency ameba 1.6.4 does not
# build on newer Crystal releases.

name: CI

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # crystal-sqlite3 links against libsqlite3 and needs its dev headers.
      - name: Install libsqlite3
        run: |
          apt-get update
          apt-get install -y libsqlite3-dev

      # Pin Crystal to 1.20.2 (ameba 1.6.4 does not build on newer Crystal).
      # If install-crystal is unavailable on the runner, replace this step with a
      # manual install of Crystal 1.20.2 (e.g. from the official tarball/apt repo).
      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: "1.20.2"

      # Route GitHub HTTPS clones through the GH_TOKEN secret so `shards install`
      # can fetch the private sonarr / qbittorrent.cr dependencies.
      - name: Configure private dependency access
        run: |
          git config --global \
            url."https://${{ secrets.GH_TOKEN }}@github.com/".insteadOf \
            "https://github.com/"

      - name: Install dependencies
        run: shards install

      - name: Check formatting
        run: crystal tool format --check

      - name: Lint
        run: bin/ameba

      - name: Build
        run: crystal build -Dpreview_mt src/main.cr -o bin/arr_janitor

      - name: Run specs
        run: crystal spec
